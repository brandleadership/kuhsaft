- Kuhsaft::Page.translation_locales.each do |locale|
  = link_to locale.to_s, new_admin_page_path(:locale => locale), :class => 'button'
= render :partial => 'form', :locals => { :url => admin_pages_path }