.link
  = link_to page.link do
    %h4= page.title
  %h5= kuhsaft.page_url(:url => page.url_without_locale)
.summary
  -if page.fulltext.present?
    %span.fulltext!= page.fulltext
  -if page.keywords.present?
    %span.keywords
      = page.keywords
