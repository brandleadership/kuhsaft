class Add<%= formatted_locale.classify %>Translation < ActiveRecord::Migration

  def change
    <%- translated_columns.each do |column_hash| %>
      add_column(:kuhsaft_pages, "<%= get_attribute(column_hash).to_s %>", :<%= get_type(column_hash).to_sym %>) unless Kuhsaft::Page.column_names.include?("<%= get_attribute(column_hash).to_s %>")
    <%- end %>
  end
end
